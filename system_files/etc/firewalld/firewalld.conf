# Toda interfaz sin zona asignada cae en "lan" (la zona restrictiva).
# La NIC fisica se asigna explicitamente el dia de la instalacion:
#   nmcli con mod <conexion> connection.zone lan
DefaultZone=lan
