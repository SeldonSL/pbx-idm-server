# Solo autenticacion por llave SSH; sin root remoto.
# El rescate sin llaves es por consola fisica (usuario core con password local).
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
